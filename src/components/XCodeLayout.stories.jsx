import { XCodeLayout } from './XCodeLayout';

const meta = {
  component: XCodeLayout,
};

export default meta;

export const Default = {
  args: {
    height: "500px",
    navigatorWidth: "15%",

  }
};